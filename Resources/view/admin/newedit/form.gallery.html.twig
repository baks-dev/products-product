{% trans_default_domain 'products.product' %}

<div class="d-flex gap-2">
	
	<div class="d-flex flex-column gap-2 w-100">
		
		<div class="card card-flush p-4">
			{# ФОТО #}
			{{ include(_self|replace({ "form.gallery.html.twig": "_photo.html.twig" })) }}
		</div>
		
		<div class="d-flex gap-2">
			
			<div class="d-flex flex-column gap-2 w-50 mb-2">
				<div class="card card-flush p-4">
					{# ФАЙЛЫ #}
					{{ include(_self|replace({ "form.gallery.html.twig": "_files.html.twig" })) }}
				</div>
			</div>
			
			<div class="d-flex flex-column gap-2 w-50 mb-2">
				<div class="card card-flush p-4">
					{# ВИДЕО #}
					{{ include(_self|replace({ "form.gallery.html.twig": "_video.html.twig" })) }}
				</div>
			
			</div>
		</div>
	</div>

</div>


